bazel_dep(name = "bazel_skylib", version = "1.9.0")
bazel_dep(name = "rules_cc", version = "0.2.20")

many_params = use_extension("//:many-params.bzl", "many_params")
use_repo(
    many_params,
    "many-params",
)
