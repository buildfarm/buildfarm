server:
  instanceType: SHARD
  name: shard
backplane:
  redisUri: "redis://localhost:6379"
  queues:
    - name: "cpu"
      properties:
        - name: "min-cores"
          value: "*"
        - name: "max-cores"
          value: "*"
    - name: "gpu"
      properties:
        - name: "gpu"
          value: "1"
worker:
  publicName: "localhost:8981"